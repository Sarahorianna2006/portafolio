@model List<portafolio.Models.Project>

<h2>Mis Proyectos</h2>

<div class="projects">
    @foreach (var p in Model)
    {
        <div class="card">
            <img src="/images/@p.Image" />
            <h3>@p.Title</h3>
            <p>@p.Description</p>
        </div>
    }
</div>